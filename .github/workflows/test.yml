name: CI

on:
  push:
    branches:
    - main
    tags:
    - '*'

jobs:

  build:
    runs-on: ubuntu-latest
    steps:

    - name: Get files
      uses: actions/checkout@v3

    - name: GitHub Tag Name example
      run: |
        echo "Tag name from GITHUB_REF_NAME: ${GITHUB_REF:10}"
        echo "Tag name from github.ref_name: ${{  github.ref_name }}"
